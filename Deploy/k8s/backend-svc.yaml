apiVersion: v1
kind: Service
metadata:
  name: catdogbe-service
  labels:
    app: catdog
    stage: dev
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    type: catdog-be
